# Getting Started

## Clone
git clone https://github.com/<you>/<repo>.git
cd <repo>

## Open
1. Unity Hub → Add → select this folder (must see Assets/ and ProjectSettings/)
2. Open with the matching Unity version
3. Wait for first import; then Window → Package Manager if packages need refresh

## Pull updates
git pull --rebase
git lfs pull
